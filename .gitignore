# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

/src/generated/prisma

.env
src/generated/

# Operator-local secrets (never commit)
.opsolid-deploy-secrets
.opsolid-smtp
.stripe-token
.claude.json
.transfer/

# Design reference bundle (HTML mocks from claude.ai/design, not source of truth)
.design-bundle/

# Deploy artifacts (built locally by scripts/deploy.sh)
opsolid-deploy.tar.gz
*.deploy.tar.gz

# Mobile app (Expo) — generated / installed artifacts
mobile/node_modules/
mobile/.expo/
mobile/dist/
mobile/web-build/
mobile/ios/
mobile/android/

# pnpm lock file (project uses npm)
pnpm-lock.yaml

/public/uploads/
